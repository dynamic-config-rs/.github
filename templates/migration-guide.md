# Migrating from <previous>

<!-- One page per breaking release, at book/src/migrations/from-X.Y.md,
     listed under Reference in SUMMARY.md. The engine's from-0.6.md is
     the worked example of this template. -->

<one sentence: how many breaking changes, and the recompile-without-edit
verdict for the common case.>

## <The breaking change, named>

<What stops compiling or changes behaviour. The exact error a user sees.
The edit that fixes it, as a diff or a before/after pair. WHY the change
earned a break — one paragraph, no apology.>

## Behaviour that changed without breaking API

<Anything a changelog reader could miss that shows up at runtime.>

## New, if you want it

<The features that rode the release, one line each, linking to their
chapters.>
